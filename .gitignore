# built binary (48MB+; rebuild with: mise run build)
/dist/

# Go build cache / test artifacts
*.test
*.out

# editor / OS noise
.idea/
.vscode/
*.swp
.DS_Store

# harness editor config (local only)
opencode.json
